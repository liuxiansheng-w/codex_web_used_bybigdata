柠檬桌面工作台体验评审 · 2026-09-18

本次覆盖会话导航、消息区、输入框、文件搜索与编辑、查询结果、变更回退、Excel/PDF 预览和设置。以下 12 项已按用户要求实施；后半部分保留改造前的评审依据。所有验收只使用桌面窗口、独立模拟服务与虚构数据，没有调用模型或执行真实 SQL。

| 项目 | 本次实现 |
| --- | --- |
| 主工作区 | 对话 / 并排 / 文件模式；工具覆盖显示，打开搜索不会挤窄编辑器 |
| 查询结果 | 紧凑工具栏与元信息；默认 1440×1000 下可见 8 行数据；保留拖动/浮动/Tab |
| 文件搜索 | 输入即分批搜索整个项目，取消文件名搜索的 4,000 项及单目录 10,000 项硬停止；进度与结果分页可见，旧请求不能覆盖新结果 |
| 变更回退 | 本次修改 / 保存历史 / Git / 代码检查分区，AI 差异直接展开，回退仍先预览和核验版本 |
| 查询管理 | 运行计时、执行时间、结果命名、查询历史、核对快照后重查和原参数预填 |
| 导航 | 压缩项目占位、无任务时隐藏空区域，常用工具固定在全局栏 |
| 消息 | 执行过程按轮次归并；用户消息复制、编辑后重发；子任务回复保持直接显示 |
| 编辑器 | 格式化直达；根据后续反馈移除选区横向操作栏，解释 / 修改 / 加入对话保留在「更多」菜单 |
| 输入框 | 附件直达，计划与持续目标按用途分组 |
| 文档 | PDF/Excel 共享文件标签，保存阅读状态；列筛选/排序/复制/筛选导出；PDF 文字选择与查找；版本检测更新 |
| 会话列表 | 时间、摘要、日期/置顶分组，标题或当前项目消息正文搜索 |
| 设置文案 | 外观归入偏好、长说明按需展开、无草稿时禁用恢复与清理 |

必要边界：SQL 服务仍没有真实取消接口，收起窗口不等于取消；一次一个远端查询。查询缓存需用户开启，最多 20 次、每次前 1,000 行、约 4 MB，截断预览不冒充完整下载；运行中记录恢复时明确标为远端状态待核对。会话正文搜索每次扫描一页 40 个，可继续加载；读取失败会提示。文档保持本机只读预览，不运行宏或重算公式。

后续改进（Excel 式列筛选）：列菜单默认列出去重值和数量，提供搜索、勾选、全选/清空/反选、确认/取消；草稿不提前影响结果，搜索后确认只保留匹配的勾选项。值数量统计所有已加载页且联动其他列条件，长列表可搜索和继续加载；空值、空字符串和普通文本分别匹配。零行结果保留表头，避免无法重新筛选。排序、条件筛选和复制仍保留，每个结果 Tab 单独记忆，开启原有结果缓存后可刷新恢复。165 项桌面回归通过；最终搜索行为调整后的 22 项专项回归及 50 个模块语法检查通过。1440×1000 浏览器验证搜索确认、取消、反选、零行恢复、多列联动、浮窗、缓存恢复、单列复制与下载筛选，域名检查通过；静态文件已生效，无需重启服务。[桌面筛选](output/playwright/checkbox-filter-desktop.png)。

后续修复（回车后光标不可见）：原自动缩进使用程序插入换行，缺少浏览器的光标滚动行为。现改用保留撤销的编辑路径，依据实际行高和编辑区高度保持光标可见，并同步行号与高亮；输入、键盘导航及采纳多行补全都适用。普通滚轮和选区事件不触发跟随，在文件中间编辑不跳到末尾。161 项桌面回归与 50 个模块语法检查通过；1440×1000 模拟查询结果面板打开时，连续 13 次回车、撤销重做、多行插入、手动向上滚动后继续编辑均通过，控制台无错误或警告。只更新前端，不重启正在运行的工作台。[底部编辑跟随](output/playwright/editor-enter-caret-desktop.png)。

后续修复（文件名搜索）：移除旧搜索的总扫描项数限制，逐批续接遍历；匹配较少时自动查完，匹配较多时每页 200 条，上一页复用已有结果。侧栏和项目工具使用同一搜索机制，切换条件取消旧请求，无法读取的目录单独提示。隐藏项、依赖、排除目录与符号链接边界保持一致。普通目录浏览及内容搜索仍有各自上限，此次不把它们描述为无限制。

后续修复（刷新后标签丢失）：将各项目文件标签和阅读位置从仅内存改为本机浏览器持久化，保持顺序、当前文件和显隐；自动恢复原布局，避免启动时恢复会话将文件视图切成并排。文件正文重新读取，其他标签按需加载；手动关闭、文件删除、延迟返回、同名跨项目文件均单独处理。只保存导航信息，未保存草稿仍受原有显式缓存设置控制，自动读取磁盘不得清除待恢复草稿。旧版本未记录的标签需重新打开一次。159 项桌面回归及 50 个模块语法检查通过；1440×1000 浏览器验证刷新、离开后重开、5 个文件标签、当前文本选区和滚动、PDF 第 2 页、Excel 第 2 个工作表、关闭后不复活，控制台无错误或警告。[文件恢复](output/playwright/workspace-restored-files.png)、[Excel 恢复](output/playwright/workspace-restored-excel.png)。此次只更新静态前端，不重启服务。

本次验证：155 项桌面回归通过；补充调整后的 11 项搜索与界面回归通过，50 个模块语法检查通过。自动化验证单目录 10,005 个文件无遗漏；1440×1000 实际浏览器验证稀疏搜索自动续接、205 个匹配分两页，以及项目工具中同样的分页行为，浏览器无错误或警告。更新空闲服务后，用实际项目目标文件名验证：9 批扫描 16,238 项、67 个匹配、0 个不可读取目录，原始 SQL 文件可找到；域名与访问控制检查通过。[侧栏分页](output/playwright/file-search-pagination-desktop.png)、[项目工具分页](output/playwright/file-search-workbench-desktop.png)。

验证完成：145 项桌面回归通过，50 个模块语法检查通过；补充编辑同步检查 3 项通过。覆盖缓存截断/恢复、只读会话正文搜索、跨项目搜索竞态、消息编辑不自动发送、执行记录节点保留、重查参数与文件隔离、文档版本检测等。1440×1000 和 1366×900 桌面操作通过，浏览器无错误/警告；未测试手机端。已在确认旧服务及其任务空闲后更新 4318，恢复 4 个会话；ningmeng.com 的 HTTPS、重定向、会话与 9 项访问控制、SSE 检查通过。

验收证据：改造后编辑器在打开工具前后均约 663 px 宽（此前降至约 453 px），结果表格滚动区由约 187 px 增至约 280 px；PDF 文本层、跨页查找与工作表切换状态已通过实际桌面操作。[主区与工具](output/playwright/upgrade-04-drawer.png)、[统一 Excel 文件标签](output/playwright/upgrade-02-excel.png)、[PDF 查找和文本层](output/playwright/upgrade-03-pdf.png)。

核心判断：当前页面按功能模块不断增加入口，同一个任务涉及多个面板、不同的操作方式和重复说明。应围绕「找文件 → 编辑 → 查询 → 看结果 → 让 AI 分析 → 核对改动」这条连续流程组织页面。已确认的冰蓝/银灰配色可以保留。

| 顺序 | 问题 | 影响 | 建议 |
| --- | --- | --- | --- |
| P1-1 | 编辑、聊天、工具同时抢占空间 | 常见桌面宽度下各区都变窄，阅读和编辑中断 | 明确主工作区，提供编辑/对话/并排布局；打开辅助工具时保留主区最低可用宽度 |
| P1-2 | 查询结果操作栏占比过大 | 有结果但看不到几行，需要先拖拽才能工作 | 压缩重复标题和元信息，常用操作保持一步可达，优先保证表格面积 |
| P1-3 | 文件搜索默认只查已加载目录 | 未展开目录里的文件查不到，还要转入第二套搜索 | 同一入口默认搜索项目文件名，显示范围、进度和截断提示 |
| P1-4 | 文件改动、保存历史、Git、代码审查混在一起 | 用户只想看改了哪里，却先读历史表单和 Git 概念 | 从文件改动进入时直接展示该文件差异；历史、Git、审查分区 |
| P1-5 | 查询等待和历史不形成完整流程 | 慢查询缺少过程反馈，刷新后结果 Tab 消失，同文件多次结果难区分 | 显示计时/明确状态；结果可命名，查询历史可恢复；真实取消需服务支持 |
| P2-1 | 侧栏和导航入口重复 | 文件/会话列表被挤到下半区，用户反复寻找入口 | 紧凑项目选择，无任务时压缩状态区，合并重复工具导航 |
| P2-2 | 普通工具执行仍逐条占聊天区 | 主结论被大量命令卡片淹没 | 按轮次汇总执行过程，保留关键进度与异常入口，答案优先 |
| P2-3 | 编辑器常用 AI 操作藏在「更多」 | 选代码后还要寻找解释、修改、加入对话 | 选区出现轻量操作条；低频方言与历史放在更多菜单 |
| P2-4 | 输入框「＋」混合多类功能 | 添加附件、设置执行方式、选工具挤在同一个菜单 | 附件作为直接动作；计划/目标与工具选择按用途分组 |
| P2-5 | 文档预览与编辑器是两套文件体验 | 打开文档改变布局，没有文件级预览 Tab；表格操作不一致 | 统一文件标签和查看器容器，按文档类型提供工具，并保存阅读位置 |
| P2-6 | 会话列表信息不足 | 相似标题难区分，搜索只支持标题 | 增加最近时间、最近内容摘要和日期分组；支持明确范围的内容搜索 |
| P2-7 | 设置与说明偏内部实现 | 日常操作被路径、协议名、实现限制打断 | 正常状态简短显示；必要限制保留在操作旁，技术详情按需展开 |

**1. 工作区布局：应先解决的基础问题。**

1440×1000 默认桌面布局中，打开编辑器后，编辑区约 663 px，对话区约 521 px；再打开项目搜索，编辑区变成约 453 px，对话区只剩约 356 px，工具面板占约 374 px。打开 Excel 预览后，剩余编辑/聊天区域还会自动改成上下排列。在 1366×900 窗口中，PDF 工具面板转到底部，只有约 306 px 高，内部文档滚动区自身却有约 585 px 高，形成嵌套滚动。

这会让用户在执行同一个任务时不断重新适应布局。建议让当前内容拥有一个稳定主区，AI 对话可停靠或收起；搜索、设置等辅助面板不再无条件增加一列。文档预览与代码文件共享主区标签。验收时覆盖三种组合：编辑+聊天、编辑+结果+搜索、文档+聊天；常用控制不遮挡，不产生无法阅读的狭窄内容区。

证据：[编辑与结果](output/playwright/audit-02-editor-results.png)、[三栏挤压](output/playwright/audit-03-three-panes.png)、[预览改变布局](output/playwright/audit-04-excel.png)、[1366 桌面预览](output/playwright/audit-09-desktop-1366.png)。实现位置：`public/style.css` 的工作区、工具侧栏、1380 px 桌面断点与文档预览规则；`public/editor-window.js`。

**2. 查询结果：数据应获得更多可见面积。**

实测结果面板约 397 px 高，表格滚动区约 187 px，只占 47%，含表头后默认约能显示五行。剩余空间被标题、结果标签、路径/引擎/时间、搜索与按钮、反馈占位、分页和 SQL 快照入口分走。排序、筛选、复制、下载、Tab 和浮动功能已经存在，问题在于默认布局和辨识度。

建议将标题/统计/窗口控制合为一行，路径和执行信息合为紧凑元信息；状态反馈没有内容时不占位。固定表头，常用复制、筛选和下载仍一步可达。验收目标：相同窗口和面板高度下，默认至少可读八行数据，筛选/排序/分页状态在切换 Tab 后保持。

实现位置：`public/sql-query.js:61`、`public/style.css` 的 `.sql-result-*`。

**3. 搜索：结果不能依赖用户先展开哪些目录。**

文件树搜索遍历浏览器已经加载的目录；用户输入后看见「已加载的目录中没有匹配项」，再点击「搜索整个项目的文件名」才转入项目搜索。提示在事实层面是准确的，但用户找文件需要了解加载机制，多了一次判断和跳转。

建议保留一个文件搜索框，默认项目范围，输入停顿后查询；内容搜索作为明确的可切换范围。扫描未完成或达到上限时说明范围，不能把部分结果写成「没有文件」。忽略目录规则仍需遵守。

实现位置：`public/file-tree.js:92`、`public/workbench.js` 的项目搜索表单。

**4. 变更回退：直接围绕当前文件组织。**

「变更与审查」上部先显示文件保存历史、文件路径输入和容量说明，然后才到 AI 修改记录；AI 修改记录仍需展开。后面又接 Git 工作区、暂存/提交、原生审查和反馈表单。查看一次 AI 修改需要在不同概念中寻找目标。

建议从消息中的文件修改入口直接定位并展示该文件差异，动作明确区分「撤销这次改动」「恢复历史版本」「Git 操作」。保留回退预览、确认和磁盘版本校验，不因为减少点击而省略这些必要步骤。文件路径从进入的文件带入；手工输入路径作为次要入口。

证据：[变更面板](output/playwright/audit-05-changes.png)。实现位置：`public/workbench.js:17`、`:30`、`renderTurnChanges()`、`showHistory()`。

**5. 查询等待与结果历史：执行之后还需要管理。**

当前显示「查询中」，没有持续计时或真实取消入口；一个查询在运行时，其他文件的查询按钮也被锁住。结果标签由序号、文件名、行数组成，同一文件反复查询时差别不明显。结果仅存在页面内存，刷新后丢失；这是现有限制，不应当把页面刷新当成解决问题的默认方法。

建议先补任务计时、后台状态和失败后的原参数重试。是否可取消必须确认查询服务的实际能力，关闭窗口不能伪装成停止远端查询。标签增加短名称/时间并支持重命名；查询语句、参数、引擎和时间可以形成历史，结果缓存另设用户可控的本机保存选项，避免默认长期保存大量业务数据。多任务并行需先验证服务能力，不以开放多个按钮冒充支持。

实现位置：`public/sql-query.js:51`、`:138`、`:251`、`:267`；`lib/sql-runner.mjs`。

**6. 侧栏与导航：减少固定占位和重复入口。**

新会话页面中，品牌、新建对话、项目工具、项目卡片、空的运行任务区、会话/文件切换与搜索占据侧栏前半段，文件列表约从 y=500 px 开始。顶部快捷工具与右侧工具导航再次出现同一组能力。无任务时的固定空状态尤其占空间。

建议将当前项目收为紧凑选择器，运行任务仅在有内容或异常时展开；文件/会话列表成为侧栏主体。固定一个主工具入口，情境操作放在对应文件、消息或结果附近。外观与主题切换属于低频偏好，可以收进设置。

证据：[初始桌面](output/playwright/audit-01-home.png)。实现位置：`public/index.html:19`、`:22`、`:23`；`public/workbench.js` 的导航。

**7. 消息区：普通执行过程也需要整理。**

子任务已直接展示，但普通命令、搜索等仍按工具事件生成独立卡片。长任务会形成连续多条命令记录。用户消息缺少就地复制/编辑后重发入口；失败后的重新尝试也没有统一的消息级操作。

建议每轮保留一份紧凑执行进度，常规步骤归入可展开的过程区，异常在附近说明影响和下一步。最终答复、改动文件与相关成果保持清楚层级。增加「复制」「编辑后重发」；重发创建新请求，不悄悄改写历史或自动执行副作用。

实现位置：`public/app.js:350` 的消息渲染与普通工具分支。

**8. 编辑器：根据当前操作显示入口。**

解释选区、修改选区、选区加入对话、格式化、静态检查、补全、历史、预览、方言、重载、保存全部混在展开后的「更多」栏。用户选中代码时，与选区相关的操作没有就地出现，展开后又一次面对十多个操作。

建议选区出现「解释 / 修改 / 加入对话」轻量操作条；格式化、查找、保存以稳定工具栏/快捷键提供；方言和其他低频操作留在更多菜单。保留未保存提示、磁盘冲突对照与版本选择，它们对防止误覆盖有明确价值。

实现位置：`public/index.html:49`、`public/editor-tools.js`、`public/file-editor.js`。

**9. 输入框：附件和执行方式应更容易分辨。**

「＋」里面同时有文件、目录、持续目标、绘图、计划模式、插件、技能及录制技能。不同用途的功能共享大弹层；常见的添加文件还要进入下一层窗口，再打开文件选择器。拖入和粘贴图片已支持，应继续保留。

建议附件入口直接可用，上传/引用的差别在需要选择时解释；计划/目标与工具选择按用途分组，并在启用后显示简短状态。权限控制保持明确，不能为了简洁变成隐藏授权。模型等已有选择可以保持紧凑，不必每次操作都展开。

证据：[添加菜单](output/playwright/audit-06-add-menu.png)。实现位置：`public/index.html` 的 `addMenu`、`attachmentDialog`；`public/composer.js`。

**10. 文档预览：统一文件体验和表格基本操作。**

PDF/Excel 在项目工具面板中打开，与代码编辑标签分离；同一时刻只有一个文档预览，切换文件/关闭后重新打开会从初始页或工作表开始。Excel 有全表文本筛选和复制筛选结果，但没有 SQL 结果的列筛选、列排序与单列复制。PDF 使用画布显示，没有正文文字选择层；可以复制整页文字或展开「本页文字」，没有文内搜索。

建议将文档纳入统一文件标签，保留页码/工作表/筛选/滚动位置。复用表格的基础交互，保留文档只读与预览截断提示。PDF 增加文字选择和查找；预览文件磁盘更新时给出清楚的版本提示或安全刷新，避免长期看着旧预览。

证据：[Excel](output/playwright/audit-04-excel.png)、[PDF](output/playwright/audit-08-pdf.png)。实现位置：`public/workbench.js:359`、`public/artifact-viewer.js:10`、`:54`。

**11. 会话列表：帮助用户认出上一项工作。**

列表展示标题和状态，缺少最近更新时间、内容摘要和日期分组；搜索明确只搜索标题。多个相似标题出现时，需要逐个打开判断。

建议增加相对时间/日期分组、最近一条内容摘要或关联文件，支持置顶分组；内容搜索必须说明是当前项目还是全部会话。标题继续以原生会话名称为准，不破坏已有同步逻辑。

实现位置：`public/app.js:223`、`public/index.html` 的会话搜索。

**12. 设置与文案：只展示当前决策需要的信息。**

普通设置顶部显示完整项目绝对路径；文件历史、查询、项目搜索等界面展示容量、扫描、协议和 API 细节；「原生 review/start」等字样直接进入操作文案。正常用户需要知道的限制被内部实现说明包围。「恢复草稿」「清除草稿」在没有可恢复内容时仍作为并列操作出现。

建议改成按需状态：有草稿显示数量和时间，没有内容时收起操作；只在达到限制、出现问题或用户展开详情时显示实现信息。依赖桌面应用的功能明确标注跳转和不可用原因。帮助和诊断仍可保留准确的协议名称，避免把品牌名称替换当成功能解决方案。

证据：[设置](output/playwright/audit-07-settings.png)。实现位置：`public/workbench.js` 的 preferences/native/git/search 文案。

推荐实施顺序：先统一主工作区和结果区域布局；随后打通搜索、编辑、查询、分析和变更核对；最后整理导航、预览一致性及设置文案。每阶段使用桌面真实操作验收，不以“按钮存在”作为体验完成标准。
