# 柠檬工作台维护约定

- Cosmos Key 保存后显示「已保存 · 自动沿用」，默认不展开输入框、不自动聚焦 Key；点击「更换 Key」才编辑，取消更换清空新 Key 草稿并沿用原凭据。仅更换服务地址时要求填写该地址对应的 Key。默认服务地址使用 `http://cosmos-api-inner.qingsonghealth.net/v1`，已有有效配置不自动迁移；凭据仍只保存在服务端，不回显或写入浏览器存储。

- 用户要求自有模型不接收无关上下文。Cosmos Chatflow/Workflow 在每个新用户轮次先发送实际聊天和精简身份说明；已识别的运行时前置 Skill、权限、项目规则、环境与工具目录留在本机按需加载。模型通过 `load_context: "workspace"` 请求后恢复原上下文，恢复前拒绝一切工具执行；同轮工具续答直接保留完整规则。只精简按 SHA-256 精确识别的运行时默认说明，自定义/未知说明和真实聊天不得截断。没有额外分类模型调用，不按“你是谁”等关键词硬编码；不跨会话缓存。普通完整文本可直接显示，不再强制闲聊回复 JSON；工具/上下文协议、兼容性验证及不完整 SSE 仍严格校验。该约定优先于下方早期“每轮完整传入”的描述。

- Cosmos 模型的身份说明必须区分模型服务和工具宿主：对外名称为「柠檬」，模型请求由用户配置的 Cosmos 服务承接，Codex 仅为本机工具执行运行时。仅在 Cosmos 出站请求中补充这一事实，保留原运行规则、权限、Skill、工具和历史正文；不改写模型回复冒充修复。工作流未返回底层模型名称时不得根据 Codex 默认提示词或别名猜测厂商/型号。新旧 Cosmos 会话与续写使用同一出口说明，Cosmos 标题在本机生成、不调用模型，原 Codex 路由不变。

- Cosmos Chatflow/Workflow 的大工具集按需加载：先提供工具名称和简短说明，模型通过 `load_tools` 请求原始完整定义，再提交执行请求。最近调用的四种工具直接提供完整定义，保留全部项目规则、Skill 元数据、对话和工具结果；小工具集及强制工具选择保持原协议，`tool_choice=none` 不携带工具定义，Responses 路由不变。说明加载仅为当前请求内的元数据选择，每次最多八个、最多两轮，不跨会话缓存，不执行工具、不改变审批；未知、重复、混合执行的说明请求报错，不自动退回全量或重复推理。未命名 Cosmos 会话使用首条消息的最多 28 字干净预览作为本机标题，省去额外的标题模型调用。体积对比只报告实测字符数，不把它冒充实际 token 账单或缓存命中。

- Cosmos 对话型应用需单独选择 Chatflow，不能调用 Workflow 的 `/workflows/run`。新配置默认 Chatflow，使用用户指定的 `http://cosmos-api-inner.qingsonghealth.net/v1`，不迁移已有配置；请求 `/chat-messages` 时同时传完整 `query` 及配置的输入变量，读取 `answer`，无需输出变量。支持 SSE 的分片 UTF-8、心跳、回复替换、错误与结束事件，结束前不执行工具；取消只尝试停止当前请求的 task_id，不重试推理。上下文由 Codex 运行时完整传入，不复用远端 conversation_id，不让标题/续写/子任务串话。上游报错只映射已知原因，不回显原始错误或敏感内容。界面解释 CHATFLOW/WORKFLOW 与节点类型的对应关系，输入变量可留空以使用 sys.query。

- 用户明确禁止使用 `ui-ux-pro-max`。
- 后续只测试桌面端，不进行手机端测试，也不新增手机端验收步骤。
- 当前界面品牌为「柠檬」，使用柠檬图标；协议名称、运行时标识与已有存储键保持兼容。
- 不停止有运行中会话或查询的服务；升级需要重启时，等待全部任务完成。
- 桌面回归命令：`node --test --test-skip-pattern="mobile|split geometry|dock drag" test/*.test.mjs`；这会排除旧套件中混合了手机视口的用例。
- 外观保留「辉光」「经典」，另提供「极光 / 深空 / 绯霞 / 曜石」，每套支持深浅色。`data-appearance` 保存具体风格，`data-skin="modern"` 复用圆角与组件样式，经典不受新皮肤覆盖。顶部外观画廊即时切换并记忆到原有 `lemon:appearance` 键；设置中的选择器同步。切换只改样式，不刷新页面、不重建编辑器/消息、不改变草稿、查询结果或布局比例。保持已有选择，未知存储值回退辉光；无需样式备份文件。
- 用户不喜欢柠檬黄；新版外观采用冰蓝、银灰，品牌图标同步使用冷色，名称仍为「柠檬」。不要再引入黄绿色作为品牌强调色。
- 除左侧目录外，编辑器、对话、终端、查询结果和工具面板在各自右上角提供独立深浅色切换，记忆到 `lemon:moduleThemes`。未单独选择的模块跟随全局主题；已选择的模块在全局切换或更换风格后保持自身深浅色。底部按钮作用于当前 Tab，终端和结果分别记忆，浮窗及关联菜单/弹窗沿用所属模块配色。切换只改外观，不重建内容、不执行查询或命令，不改变草稿、筛选或滚动位置；代码高亮、文字和控件必须同步适配。
- 用户要求各类框使用圆角：当前外观统一采用 8/10/14/16/20 px 的圆角层级，覆盖控件、消息、菜单、卡片、停靠/浮动面板和弹窗。代码文字层、表格单元格、滚动与拖动热区保持完整，不通过全局裁切破坏交互。
- 主区不显示贯穿页面的硬分隔线：侧栏、顶栏和停靠区用背景及间距区分；分隔条保持原有拖拽和键盘操作，仅在悬停、聚焦或拖动时显示短圆角手柄。编辑文本获得焦点时不画直角描边，改为状态栏背景提示；浮窗与表格的必要边界仍保留。
- 用户指定 `ningmeng.com` 仅在当前 Mac 使用。域名代理只绑定回环地址，转发到 4318；不配置公网 DNS，不开放局域网，不放宽来源/会话/CSRF 校验。证书私钥只放在被忽略的本机目录或系统运行目录。
- 项目实体位于 `临时任务/data_request/刘正阳任务/AI/codex-web-shell`，仓库根目录的 `codex-web-shell` 是兼容符号链接。维护时使用实体目录；默认工作目录仍为 `dw_job`。
- 已有会话名称以原生 `thread.name` 和 `thread/name/updated` 为准；未命名的新会话及打开的历史会话根据首条消息生成简短标题，并用原生改名接口同步。Cosmos 在本机截取干净预览作为标题、不调用模型；Codex 标题生成期间先显示去除附件说明的短预览，标题辅助轮次必须临时、只读、禁用工具，不污染正常聊天和历史列表。生成失败不影响任务，不覆盖手动名称或较新的原生改名。
- 用户图片应显示缩略图并支持大图预览；不要把自动生成的图片路径说明展示在正文。历史图片仅从原生附件记录恢复，预览使用经会话认证的不透明编号，不增加任意路径读取或外部图片抓取接口。
- 普通文件与文件夹在消息中显示圆角附件卡片，默认只显示文件名/类型，完整路径收进「查看路径」。兼容历史消息末尾精确的自动附件说明，不改原始消息和模型输入，不把普通路径文字或代码示例当附件。项目内附件复用现有编辑器/预览入口，渲染卡片不读取文件，不为项目外文件增加任意路径读取接口。复制与编辑重发只使用清理后的正文，重发附件需重新选择。

- 每次 SQL 查询保留独立结果 Tab，同项目跨文件切换可见，各自保留筛选/排序/分页和自定义名称。当前标签最多 20 个/项目，满额提示用户关闭，不静默覆盖。结果默认仅在内存；用户可明确开启本机缓存，最多最近 20 次、每次 1,000 行、约 4 MB，关闭后清除缓存。截断预览必须明确标记，没有完整下载链接时不能用「下载全部」冒充完整数据。刷新后的运行中记录标为远端状态待核对，不能自动重试。重查先核对快照，不覆盖编辑文件。命名参数按项目和引擎在本机浏览器记忆，匿名位置参数仅限同一 SQL；只有确认查询才更新参数值，取消不改写。

- 参数名称精确为 `bdp.system.bizdate` 时，点击或聚焦文本输入框直接展开同主题日历，不另设日历按钮。支持年月选择、昨天快捷选择、手输同步定位、键盘导航；选中即收起，Esc/点击外部收起。新查询按浏览器本地日历计算昨天，不继承旧记忆日期；历史结果重查保留快照中明确的日期。日历和完整手输日期提交为 `YYYYMMDD`，辅助控件不作为 SQL 参数发送。选日期不自动查询，取消不更新记忆，其他参数继续按原方式输入与记忆；不改 SQL 或原执行接口限制。

- SQL AI 续写仅发送当前编辑快照的有限上下文（光标前 12000、后 4000、必要时文件头 4000 字符），复用现有登录与所选模型。辅助请求临时、只读、禁用工具/插件/MCP/记忆，不进入聊天或执行 SQL；每次最多一个请求，取消/超时必须清理独立进程，不中断主会话。灰字不写入 textarea，只有用户 Tab 采纳才改未保存草稿；保留浏览器原生撤销，光标/内容/文件/项目变化时旧返回失效，无法确定的表字段/业务口径返回空建议。常规 UI 测试用模拟续写，真实模型验收仅使用虚构结构。
- AI 续写按编辑快照和光标事件触发，不为 `dt`、日期参数、JOIN 或特定业务字段硬编码续写规则。输入、点击、键盘移动、恢复焦点、采纳后均可在 350 毫秒停顿后请求。沿着灰字输入只消耗对应前缀；缓存仅保留本页内存中的最多 20 条精确上下文，区分项目/文件/前后文/模型/引擎，有效建议 60 秒、空建议 10 秒。Esc 忽略后不在同一上下文自动重弹，手动触发可重试。通用插入补全与 Next Edit（改写、跳转）是不同能力，不宣称已实现后者。
- 模型续写的等待秒数、空建议和失败状态直接显示在原有顶部 AI 控件，空建议/失败旁提供可用键盘访问的「重试」。详细状态仍在底栏；不增加独立横向工具栏，不用状态显示改进冒充模型速度改进。遇到用户反馈“没有建议”，需分别核实触发、真实模型结果及灰字显示，不能只依赖模拟输出测试。

- 文件回退必须先展示服务器生成的差异预览，再确认并校验磁盘版本；不能覆盖未保存草稿。网页保存历史本机持久化，保留上限必须明确；新建撤销移到废纸篓。不要将「恢复 Git HEAD」当作「撤销本次 AI 改动」。
- 用户明确打开 `AIdata` 下的 Excel 导出文件时，允许通过现有只读成果/工作簿预览接口读取 `.xlsx` / `.xls` / `.xlsm`。目录忽略只在此预览场景例外，不扩大自动目录扫描、搜索、文本读写或附件引用范围；隐藏子目录、依赖目录、符号链接和项目外路径继续拒绝，沿用原会话/来源/CSRF 与大小校验。
- Excel 工作表第一行默认作为表头，不参与数据行筛选、排序、统计或重复导出；空表头显示列号兜底，重复表头以原列号区分内部身份。查看交互与 SQL 结果一致：去重值及数量勾选、搜索、全选/清空/反选、确认/取消、条件筛选、排序、分页、行/列/表头复制、筛选 CSV 导出和选区统计。筛选、复制覆盖所有已加载页，选区统计只计算当前页，截断预览须明确显示已加载范围。工作表各自保留筛选、排序、分页和滚动；只把阅读位置及每页行数纳入导航缓存，不持久化单元格、筛选值或选区。不执行宏、公式、SQL 或模型请求；关闭预览释放菜单、监听器和拖选状态。
- Excel/PDF 预览使用本机依赖，不上传文件；Excel 仅显示已有数据/缓存结果，不运行宏或重算公式。子任务面板只展示原生记录和实际状态，打开面板不创建子任务，停止必须确认父子归属并固定当前活动轮次。
- 用户要求暂不实施表结构驱动的 AI 续写，不接入元数据服务。
- 终端与查询结果共用底部面板，通过「终端 / 查询结果」Tab 切换；并排停靠时仅占编辑器列下方，右侧对话始终使用完整高度，底部宽度跟随左右分栏。纯对话或编辑器浮动时，展开/拖动上限必须给完整输入区和可读消息预留空间，并随多行输入和窗口尺寸调整；收起后保留紧凑 Tab 栏，可拖动高度、展开/还原，记忆打开状态、当前 Tab 与高度。切换只隐藏既有节点，不清除输出、查询标签、筛选或草稿，不停止/重启任务；新查询开始时显示结果，完成时不得抢走当前终端 Tab。终端按项目隔离，同项目切换会话保留命令草稿和输出；迟到的轮询响应不得混入其他项目。结果保留独立浮窗，回停靠放入底部面板。终端使用连续命令行界面，提示符后直接输入并按 Enter 执行，不再展示提交按钮或重复确认弹窗（用户已明确要求）；Enter 是手动执行授权，后端仍校验 confirmed、权限与限额。↑↓ 历史、Ctrl+C 中断、Ctrl+L 清屏，运行中同一输入位置发送行式 stdin。历史/草稿/写权限按项目隔离，粘贴和输入法选字不自动执行，失败保留输入且不自动重试。写权限收进右上角终端设置，默认只读；仍为原有受限执行器，不宣称完整 PTY，不放宽权限、网络或运行上限。
- 用户已取消选中代码后出现的横向操作栏（已选字符 / 解释 / 修改 / 加入对话）；不要重新加入，相关操作保留在编辑器「更多」菜单。
- Markdown 文件默认预览，「预览 / 源码」放在编辑器操作栏「更多 ⋯」紧邻左侧，不放在文件标签栏；格式化等操作仍位于左侧。两种模式共用当前文本缓冲区，包含未保存修改；切换保留原 textarea、选区、撤销和独立滚动位置，按文件仅持久化模式与阅读位置。查找/显式行号跳转进入源码，普通文件链接保留预览选择；磁盘同步复用现有草稿合并规则。渲染继承编辑器主题，HTML 惰性；相对文件链接以文档目录为基准、不得越过项目根，复用文件读取接口，不新增任意路径读取或自动外部资源加载。
- 文档预览使用本机 markdown-it、KaTeX MathML 和 Mermaid，构建源码位于 `browser/`，改动后运行 `npm run build:markdown`，生成并保留 `public/vendor/markdown/` 资源。聊天继续使用原渲染器。支持目录、嵌套/任务列表、删除线、脚注、表格、公式、代码高亮/复制；Mermaid 图表可缩放、放大、查看源码及下载 SVG，切换编辑器深浅色重新绘图。图表测量运行于独立同源页面，其 CSP 允许排版样式但禁止网络请求与外部图片；主页面 CSP 不放宽，严格模式输出经清洗后只作为 SVG 图片显示。相对图片只经已有受认证的成果接口读取，项目/隐藏/符号链接边界不变；外部图片默认提供打开链接，不自动请求。异步结果按预览版本隔离，关闭/替换预览释放监听器和 Blob URL；错误图保留源码及重试入口。
- 默认在本机浏览器记住各项目打开的文件标签、顺序、当前文件、显隐、语言、光标与阅读位置；刷新或重新进入后恢复。记录只包含导航信息，不缓存文件正文；文本、PDF、Excel 从磁盘重新读取，非当前标签按需加载。已关闭的标签不能复活；无法读取的文件保留错误提示，不能阻塞其他标签。自动恢复会话不能把「文件」布局强行切回「并排」。未保存草稿和查询结果仍遵循各自已选择的缓存设置，自动打开磁盘文件不能清除待恢复草稿。
- 编辑器 Tab / Shift + Tab 增减当前行或选中多行的缩进；普通 Tab 优先采纳补全，不能覆盖多行选区。⌘/Ctrl + / 在编辑器内切换 SQL/Python 行注释，编辑器外仍搜索会话。修改仅进入草稿并保留浏览器撤销；Esc 关闭补全后，Esc 再接 Tab 可移出编辑区，避免键盘焦点陷阱。
- 回车自动缩进须保留原生撤销，并在输入、粘贴多行、采纳补全或键盘移动光标后按编辑区实际高度让光标保持可见；行号与高亮同步滚动。不要在普通滚轮、选区变化、后台同步或恢复阅读位置时强行滚回光标，也不能始终跳到文件末尾。

- 子任务的状态、任务说明与最新回复直接展示在主对话；不要增加「查看结果」折叠层。同轮重复协作事件合并，自动同步不重建卡片、不打断选中文本；进入完整会话后提供直接返回主任务入口。

- 桌面提供「对话 / 并排 / 文件」主区布局，工具面板覆盖显示，不压窄现有编辑区。PDF/Excel 使用同一文件标签容器；切换保留阅读状态，磁盘自动更新按版本隔离。普通工具过程可按轮次折叠，AI 子任务结果仍直接显示。
- 文件名搜索默认整个当前项目，使用可续接的分批遍历，不设置总扫描项数上限；匹配较少时自动继续，结果每页 200 项，翻页不能丢失匹配。切换搜索或项目取消旧请求，忽略目录与符号链接边界不变；会话正文搜索仅只读读取分页内的用户/助手消息，不调用模型或恢复会话，不搜索命令输出，不把读取失败当作无匹配。

- SQL 结果列筛选默认提供 Excel 式去重值/数量勾选、搜索、全选/清空/反选、确认/取消；草稿操作不能提前影响结果。搜索后确认只保留搜索匹配的勾选项；不搜索时批量操作覆盖全部值而非当前 DOM 批次。空勾选表示 0 行，保留表头以便恢复。按条件筛选和复制功能仍可用；值数量跨所有已加载页、受其他列筛选影响，截断预览不得暗示完整统计。筛选按结果 Tab 独立，仅在已有结果缓存明确开启时随之持久化。

- 查询结果支持单元格矩形拖选、Shift 扩选、方向键和本页全选；现有底栏右侧实时显示计数、数值个数、求和、平均值、最大值、最小值。统计区固定预留高度，显示与清除时不挤动表格或翻页按钮；只有实际拖动才启动鼠标捕获及边缘滚动，保留原生单击、双击。计数包括选中空格，数值计算排除 NULL、空白、文本和布尔值，保留 SQL 数字字符串的小数及大整数精度，舍入的平均值标记「≈」。选区仅针对当前页已展示数据，按结果 Tab 在内存隔离；筛选、排序、翻页或改变每页行数时清除，不计算旧坐标对应的新数据。支持 Ctrl/⌘+C 复制选区和 Esc 取消，保留原有双击复制单元格及列/行复制。统计不执行查询、不调用模型、不更改原值，也不将选区写入本机结果缓存。

- 会话导航集中在右侧聊天模块：列表、搜索、新建与当前对话共用同一区域，顶部返回箭头切换列表/对话；左侧仅保留项目和文件，不再放会话 Tab 或新建会话按钮。列表默认紧凑展示标题、更新时间及必要状态，正文搜索时显示摘要。视图切换仅隐藏既有消息/输入节点，保留草稿、阅读位置和运行任务；选中已打开会话也必须返回聊天视图。⌘/Ctrl + / 在编辑器外打开右侧会话搜索，不展开左栏；新建仍为 ⌘/Ctrl + K。保持既有文件/并排/对话布局和底部面板范围。

- AI 回复的 Markdown 文件引用兼容 `[文件名](<完整路径:行号>)`、中文/空格/括号路径及 `#L行号`。渲染时只生成可点击引用，用户点击后复用当前项目编辑器并定位对应行；不提前读取文件，不改写已有编辑草稿，不执行 SQL。代码示例和不完整链接保持文字，HTML 与危险协议保持惰性；沿用当前项目与文件访问边界，不新增任意本机路径读取接口。
- 对话中每张 Markdown 表格右上角常驻「复制表格」，复制范围仅为该表的已显示表头和全部数据行。优先同时写入安全重建的 HTML 表格与 TSV，兼容 Excel/文档粘贴；不支持富格式时退回 TSV。复制不带周围消息、按钮或文件链接路径，不执行公式；保留空单元格和显示数值精度。历史、流式更新及子任务中的表格使用同一个委托监听器，防重复点击，成功/失败显示反馈；不触发模型或查询，不改变整条回复与代码块复制。

- 编辑器当前文件与左侧目录联动：打开、切换标签、从回复/搜索跳转及恢复文件时，只沿该文件的祖先路径分页加载并展开，滚动目录至文件并高亮，不抢走编辑光标。快速切换文件或项目、手动搜索/折叠时使旧定位失效；普通输入和后台刷新不得反复展开或滚动。Tab 标题支持选中文本复制，并提供右键/悬停「⋯」菜单复制文件名、相对路径、完整路径，以及在目录中定位；复制非当前标签不得切换文件或改写草稿，菜单沿用编辑器独立主题。

- 对话输入框下方的账户额度与草稿、token/上下文、快捷键处于同一行，不新增横栏。只使用现有认证的 `/api/usage` → `account/rateLimits/read` 返回值计算剩余比例；按真实周期区分主/次窗口，不把 token、积分余额或缺失字段换算成额度。悬停显示周期与重置时间，点击刷新；可见页面每分钟更新，恢复焦点及任务完成时节流刷新。错误、断连或不支持的登录方式显示未知，不保留过期百分比或写入本地存储；不为读取额度调用模型、重启服务或改变登录。

- 编辑器查找在当前未保存文本上进行，保持区分大小写的字面匹配；输入即显示高亮和当前项/总数，Enter / Shift+Enter 向后/向前循环，查找框保持焦点。跳转同时保证行与长行匹配可见，Esc/关闭清除搜索装饰并回到编辑器。可见区域匹配使用与语法高亮独立的安全文本标记，大文件按视口绘制，计数与导航仍覆盖全文；文件/内容改变后重新计算，不修改草稿、不自动保存或执行。输入法确认键不触发查找；替换全部保持明确点击和确认，替换文本中的 `$` 按字面处理。

- 外部文件更新使用初次读取的基线、当前草稿和最新磁盘文本做保守的三方合并。不同位置且能明确对齐的修改自动进入编辑区，仍为未保存草稿；相同修改去重，重叠改动、歧义重复段落或超限结果不合并。更新保存版本前保留合并前快照，底栏可撤销最近一次合并；合并后有新输入时禁用该撤销，撤销后不能在下一次轮询重复合并相同磁盘版本。真正冲突只展示一条紧凑提示，可只读查看刚读取的最新代码或对照草稿；不自动覆盖草稿或写入磁盘，不抢焦点，不接受过期读取响应。对照/最新代码弹窗跟随编辑器独立主题。

- 用户要求去掉网页这一层的 SQL 拦截，同时保留原接口限制：页面后端只校验请求格式、参数和传输大小，不再按 SQL 类型、关键字或语句数量拒绝；原内容交给现有 DGC runner，由原接口判断是否允许。不得因此修改 DGC runner 或 cloud-sql-exec 的限制。保留 `--lzy`、登录/来源/CSRF 校验和原接口错误，不自动重试。非查询语句使用「执行」状态，仅在原接口确认成功后显示成功；测试只用模拟执行服务。

- 审批区在多条命令/文件修改请求时提供「全部允许一次」，保留逐条允许/拒绝。批量范围固定为点击时当前会话已展示的请求，复用逐条认证接口，只发送 `accept`；不包含新增请求、补充问题、插件交互或整轮权限扩展，不更改权限模式。重复点击不得重发，已结束/变化请求跳过，断线或切换会话后停止剩余提交，失败可手动处理且不自动重试。保留其他请求表单的输入和焦点；权限扩展单独标注为本轮授权。测试使用模拟请求，不批准真实待办。

- 当前生产 4318 由用户 LaunchAgent `com.local.ningmeng-workbench` 管理，配置在 `~/Library/LaunchAgents/`，登录时启动、进程退出后自动恢复。不要再用 `.local/activate-*.mjs` 或聊天工具的临时进程接管生产端口。后端升级需先核对运行中会话、审批、子任务、终端及 SQL 请求，再通过 launchd 维护；静态资源更新不重启服务。LaunchAgent 启动目录使用用户主目录，应用以项目绝对路径加载，`WORKSPACE_CWD` 指向 `dw_job`。日志在 `~/Library/Logs/Ningmeng/workbench.stdout.log` 和 `~/Library/Logs/Ningmeng/workbench.stderr.log`，权限 0600，不输出认证信息。

- 实时连接首次发送完整快照，支持 `version=2` 的页面随后按消息 ID/会话版本接收增量；保留旧页面的完整事件兼容。响应流背压必须等待 drain，积压时每个会话只保留最新状态，不能因为历史超过固定字节数就断连；无进展超时才释放慢连接。页面自动退避重连，服务重启导致 401 时仅通过同源首页续会话并重新读取 CSRF，不刷新整页、不清除编辑/聊天/审批表单草稿，也不自动重发消息、审批、SQL 或命令。旧连接和过期读取不得覆盖新状态，恢复时只读核对活动会话。

- 左侧项目列表每一行都常驻显示「⋯」菜单，内含「Git 提交与推送」，顶部入口继续保留。左侧入口读取所点击项目的 Git 状态，不通过切换项目清空聊天或编辑草稿；Git 说明、勾选和请求状态沿用按项目隔离。菜单浮层不能被项目列表滚动区裁掉，支持键盘、Esc 与外部点击关闭；打开入口不自动提交或推送。

- 左下角提供 Codex / Cosmos 模型服务切换。默认 Cosmos 工作流地址沿用现有示例，高级设置可调整地址、变量名或使用 Responses 模型接口。新 Key 必须经过无项目数据的两轮工具调用/续答兼容测试才原子保存并切换；HTTP 200 或普通 SQL 文本不代表兼容，失败保留原配置，不回退到 Codex、不重试推理。Key 只保存于被忽略的 `.local/model-provider/connection.json`（目录 0700、文件 0600），不回传、不放进浏览器存储、模型提示词、运行时参数或日志；地址变化时必须重填 Key，禁跟随重定向。切换进入新对话并保留旧会话、草稿、编辑器和任务；旧运行时连接固定原凭据，读历史不切换服务。聊天、续写、标题和子任务通过相同提供商路由，原 Codex 权限和审批继续生效，适配层只转换协议、不执行工具。Cosmos 工作流支持文本及工具协议，不宣称原生多模态、云端工具或全部 Codex 能力；Cosmos 下不显示 Codex 额度。生产加载仍须等待全部任务与审批结束。UI 与回归测试使用虚构 Key 和模拟 Cosmos 服务，不读取或调用真实 Key。
