# 自动补全方案调研与当前实现

调研日期：2026-09-18。目标是理解用户下一步要写或改的代码，不把截图中的日期条件、别名或关联条件做成固定模板。

## 公开方案如何实现

| 环节 | 官方资料与开源实现 | 对柠檬的意义 |
| --- | --- | --- |
| 补全与编辑预测 | Cursor Tab 使用周围代码、近期编辑和诊断信息，能建议多行修改以及下一处编辑位置。[Cursor Tab 文档](https://prod.cursor.com/help/ai-features/tab) | 只在光标处插入字符串，无法完整复现改别名后联动修正其他位置的体验。 |
| 专门的模型任务 | Cursor 在 2025 年介绍过用于编辑预测的专用模型；这是历史架构说明，不代表公开了全部当前实现。[Cursor 模型文章](https://cursor.com/en-US/blog/tab-update) | 缩短前端定时器或增加几个提示词，不等同于获得专用模型的响应速度和编辑预测能力。 |
| 前后文补全 | Continue 将 autocomplete 作为独立模型角色，说明了 FIM：给模型光标前后文，预测中间缺失内容。[模型角色](https://docs.continue.dev/customize/model-roles/autocomplete) | 应把低延迟补全模型与长任务聊天模型分开设计；当前选中的通用模型可以保留为兼容方案。 |
| 上下文选择 | Continue 除光标前后文，还选择相关定义、导入符号和近期文件片段。[上下文选择](https://docs.continue.dev/ide-extensions/autocomplete/context-selection) | 有效上下文需要相关性和长度预算，不能直接把整个项目塞入提示词。当前项目仍遵守只发送本文件有限上下文的约定。 |
| 请求生命周期 | Continue 使用防抖、缓存和输出后处理；源码还体现了取消、上下文检索、生成和结果记录等分层。[工作原理](https://docs.continue.dev/ide-extensions/autocomplete/how-it-works)、[CompletionProvider 源码](https://github.com/continuedev/continue/blob/main/core/autocomplete/CompletionProvider.ts) | 持续编辑时的体验依赖整条流程，不能只检查模型是否偶尔能生成正确 SQL。 |

以上为公开资料可确认的能力，不推断 Cursor 未公开的模型权重、完整提示词或内部服务协议。

## 本地代码检查结果

当前路径为 `public/editor-tools.js` → `/api/project/complete` → `lib/sql-completion.mjs`。

- 已有关键字提示和 AI 逻辑续写两种功能。AI 会收到当前文件光标前后文，不只是匹配单词。
- 服务端每次创建独立、临时、只读的辅助运行时；等待完整结构化结果后返回。它复用现有模型登录，不是独立的 FIM 或 Next Edit 服务。
- 原前端主要由输入事件触发；移动光标可取消请求，但不完整地重新触发。采纳后不会主动继续，也没有结果缓存和沿灰字输入的复用。
- 当前输出只能插入光标位置。没有已有代码的替换范围、编辑轨迹、跨文件相关性检索和下一位置跳转。
- 一次使用虚构表结构的真实模型诊断能生成前文已有的日期参数，端到端耗时约 5.7 秒。它只证明该示例可完成，不是平均延迟、成功率或 Cursor 对比基准。

## 本次已落地

1. 通用编辑事件触发：输入、鼠标定位、键盘移动、恢复焦点、采纳本地词或 AI 建议后都可以请求，不使用 SQL 关键词触发白名单。
2. 停顿时间从 1200 毫秒调整为 350 毫秒。连续输入合并请求；旧请求取消和快照校验保留。
3. 精确上下文内存缓存：最多 20 条，建议 60 秒、空结果 10 秒；项目、文件、全文与光标、模型、方言必须一致。缓存不持久化、不增加发送范围。
4. 用户输入匹配灰字开头时保留剩余部分；采纳后按新内容预测下一段。不会盲目预取其他编辑位置。
5. Esc 后同一上下文不重复弹出；手动触发允许重试。空结果、连接问题和不可用格式在原状态栏反馈。
6. 仍以 Tab 确认写入未保存草稿，支持原生撤销，不自动保存或查询。

## 后续架构方向与验收标准

下面是方案，不是本次已完成能力：

- **独立补全提供方**：定义带取消信号的补全接口，明确是否支持前后文、流式输出、编辑替换、位置预测。先测首条可用建议时间和质量，再决定使用现有运行时还是适配专用补全模型，不能只按模型规模判断。
- **上下文构建器**：在现有文件范围内优先组织当前代码块、注释、别名及近期修改。若以后扩展到其他文件，应按相关性取有限片段并设置范围；用户暂缓的数据库元数据接入仍不实施。
- **结果处理**：对候选去重，识别与后缀重复的完整片段，保留缩进，丢弃不完整或无效输出。流式建议必须保持版本检查，不能把尚未解码完成的 JSON 直接显示为代码。
- **Next Edit 独立能力**：建议包含精确旧版本和替换范围；先显示差异，用户采纳后再应用，并允许一次撤销。跳转到下一处建议与自动修改应分开，不能把普通字符串插入包装成“全局智能修改”。
- **统一评测集**：使用虚构结构覆盖表达式、参数、CTE、聚合、窗口函数、JOIN、别名重命名、已有后缀、注释意图以及不足上下文。统计首次/重复建议延迟、有效率、取消率、采纳率和错误覆盖次数；不只验收截图中的两段 SQL。

## 验证

- 13 项补全交互单测通过，覆盖通用光标触发、连续采纳、沿灰字输入、缓存隔离、Esc、空结果、旧请求取消、上下文限制等。
- 项目要求的桌面回归共 172 项通过；50 个模块通过语法检查。
- 1440×1000 桌面浏览器验收通过：多行预览不改正文、沿灰字输入、Tab 连续采纳、保留后缀、原生撤销、退格复用缓存、Esc 忽略及手动重试；页面错误为 0。截图在 `output/playwright/inline-general-preview.png`、`output/playwright/inline-general-cache.png`。
- 真实模型诊断只使用虚构结构；常规界面测试使用模拟补全，不执行 SQL。

## 同日复查：子查询别名后回车没有灰字

用户再次反馈后，单独检查回车路径、文件自动检查和真实模型请求：

- 桌面模拟慢响应证明 `) bb` 后回车会提交新行的前后文；跨过多次文件自动检查后，请求仍有效并能显示灰字。
- 两次同结构虚构数据诊断中，一次达到 30 秒服务超时，一次约 13 秒返回关联条件。不能据此认定截图当时一定是超时，因为未取得该页面当时的请求状态。
- 进一步对默认模型调用计时：运行时及配置读取约 0.34 秒；辅助会话约在 5.64 秒准备好；首段文本约在 12.95 秒出现，13.33 秒完成。该次默认模型为 GPT-6-Astra，续写轮次使用 low 推理档位。这些是单次观测，不是平均值。
- 独立桌面页面通过真实模型端到端验收：回车后约 9.96 秒显示 `ON aa.user_id = bb.user_id`，灰字不改正文，Tab 精确采纳，仅 1 次模型请求，没有保存或执行 SQL。全部表和关联关系均为明确给定的虚构数据。截图：`output/playwright/inline-live-result.png`。

本次把等待秒数、无建议和失败直接显示在原顶部 AI 控件，并为无建议/失败增加「重试」，无需寻找快捷键。底栏继续保留详细原因。这些变更解决反馈不明显的问题，没有消除辅助会话和模型生成延迟，也没有更换用户模型。

新增回车、等待计时、超时重试及键盘访问测试；当前补全单测 16 项、桌面回归 175 项通过，50 个模块通过语法检查。
